project:
  name: surface-defect-detection
  version: 0.1.0
  description: MVTec AD surface-defect benchmark with supervised, anomaly, VLM, and hybrid comparisons.

paths:
  data_root: data
  raw_data_dir: data/raw
  interim_data_dir: data/interim
  processed_data_dir: data/processed
  reports_dir: data/reports
  artifacts_dir: artifacts
  model_artifacts_dir: artifacts/models
  heatmap_artifacts_dir: artifacts/heatmaps
  benchmark_artifacts_dir: artifacts/benchmarks
  mlflow_tracking_uri: mlruns

experiment:
  current: mvtec_benchmark
  name: MVTec Benchmark


