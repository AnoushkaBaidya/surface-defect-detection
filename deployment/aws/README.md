# AWS Notes

This directory is intentionally minimal.

It exists to hold cloud-specific packaging only if the lightweight runtime path is promoted into a managed deployment target. No active AWS deployment workflow is part of the benchmark results in this repository.
