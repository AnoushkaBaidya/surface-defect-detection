# Local Runtime Notes

This directory is reserved for local packaging of the lightweight inference runtime.

The active runtime surface currently lives in:

- `src/defect_detection/runtime/`
- `src/defect_detection/inference/`
- `src/defect_detection/api/app.py`
