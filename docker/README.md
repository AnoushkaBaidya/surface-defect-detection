# Container Notes

Container assets are kept separate from the benchmark core so the repository does not imply a larger serving platform than the code currently implements.

If container packaging is added, it should wrap the existing runtime path rather than introduce an alternate execution flow.
