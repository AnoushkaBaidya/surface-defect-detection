# Notebook Notes

The public benchmark repository keeps exploratory notebook work out of the main execution path.

The implemented and supported workflow lives in:

- `scripts/`
- `src/`
- `configs/`
- `reports/`

This directory remains available for future local exploration without becoming part of the benchmark contract.
